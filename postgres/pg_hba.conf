# Allow local development tools (including Prisma) to connect through Docker.
local   all             all                                     trust
# Docker Desktop may proxy localhost connections from an internal address.
# The Compose port binding limits this service to the host machine.
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::0/0                   scram-sha-256
